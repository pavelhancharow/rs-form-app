import styled from 'styled-components';

const NotFoundUI = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.2rem;
  color: var(--accent-primary);
`;

export default NotFoundUI;
